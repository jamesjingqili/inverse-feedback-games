using iLQGames

max_iteration = 1000
cost = zeros(n, max_iteration)

for iteration in 1:max_iteration
	
end

# for iteration in range(max_iteration)
# 	# 1. Every 100 iterations, compute iLQ OLNE and FNE solutions. Otherwise, going direct to step 2.
# 	# 1.1. Compute p(x^*  |  FB, cost[:,iteration] ) and p(x^*  |  OL, cost[:,iteration] ), and update belief
# 	# 2. Compute gradient: the state trajectory predicton loss  w.r.t.  cost[:, iteration]	
# 	# 3. terminate when state trajectory prediction error is suffient small
# end
# cost 

